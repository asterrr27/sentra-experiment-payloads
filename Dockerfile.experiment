FROM python:3.12-slim

WORKDIR /experiment

COPY requirements_lock.txt .
RUN pip install --no-cache-dir -r requirements_lock.txt

COPY . .

ARG GROQ_API_KEY
ENV GROQ_API_KEY=${GROQ_API_KEY}

EXPOSE 8001 8002 8003

CMD ["python", "experiment_runner.py", "--help"]
